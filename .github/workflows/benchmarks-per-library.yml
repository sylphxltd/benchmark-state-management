name: Auto-Benchmark and Update

on:
  schedule:
    # Run every hour at minute 0
    - cron: '0 * * * *'
  workflow_dispatch:
  push:
    paths:
      - 'benchmarks/**'
      - 'scripts/**'
      - '.github/workflows/benchmarks-per-library.yml'

jobs:
  # ============================================================================
  # Run benchmarks and update README
  # ============================================================================
  benchmark-and-update:
    name: Benchmark & Update (state-management)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install root dependencies
        run: npm ci

      - name: Install state-management dependencies
        working-directory: benchmarks/state-management
        run: npm ci

      - name: Check versions and bundle sizes
        id: check
        run: |
          echo "🔍 Checking library versions and bundle sizes..."
          npx tsx scripts/check-versions.ts benchmarks/state-management
          echo "✓ Version check complete"

      - name: Run benchmarks
        if: steps.check.outputs.has_updates == 'true'
        run: |
          echo "🚀 Running benchmarks..."
          cd benchmarks/state-management

          # Get updated libraries from check step
          UPDATED_LIBS='${{ steps.check.outputs.updated_libs }}'

          # If specific libraries were updated, only benchmark those
          if [ -n "$UPDATED_LIBS" ] && [ "$UPDATED_LIBS" != "[]" ]; then
            LIBS=$(echo "$UPDATED_LIBS" | jq -r 'join(",")')
            echo "📦 Updated libraries: $LIBS"
            npm run benchmark -- --libraries="$LIBS"
          else
            echo "📝 Test files changed, running all benchmarks"
            npm run benchmark
          fi

          echo "✓ Benchmarks complete"

      - name: Generate category README
        run: |
          echo "📝 Generating category README..."
          npx tsx scripts/generate-simple-readme.ts benchmarks/state-management
          echo "✓ Category README generated"

      - name: Generate root README
        run: npx tsx scripts/generate-root-readme.ts

      - name: Check if there are changes
        id: changes
        run: |
          git diff --quiet || echo "has_changes=true" >> $GITHUB_OUTPUT

      - name: Commit and push results
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # Add all changes (benchmark results, versions, README)
          git add benchmarks/ README.md

          # Create commit message
          COMMIT_MSG="chore: auto-benchmark and update results"

          git commit -m "$COMMIT_MSG" || echo "No changes to commit"
          git push || echo "Nothing to push"
